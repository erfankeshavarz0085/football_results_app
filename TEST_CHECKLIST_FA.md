# چک‌لیست تست Shoot Ball

از این فایل برای تست نهایی پروژه بعد از تغییرات مهم یا قبل از انتشار در GitHub استفاده کن.

## تنظیمات اولیه

- [ ] فایل `.env` در ریشه پروژه وجود دارد.
- [ ] برای تست API واقعی، مقدار `API_KEY` تنظیم شده است.
- [ ] با `API_ENABLED=true` اطلاعات واقعی API دریافت می‌شود.
- [ ] با `API_ENABLED=false` درخواست واقعی به API ارسال نمی‌شود.
- [ ] با `API_DEMO_FALLBACK_ENABLED=true` دیتای نمونه فعال است.

## شروع برنامه

- [ ] صفحه Splash نمایش داده می‌شود.
- [ ] زمان نمایش Splash درست است.
- [ ] بعد از Splash صفحه Home باز می‌شود.
- [ ] نوار پایین برنامه نمایش داده می‌شود و کار می‌کند.

## صفحه Home

- [ ] بازی‌های تاریخ امروز نمایش داده می‌شوند.
- [ ] دکمه روز قبل تاریخ بازی‌ها را تغییر می‌دهد.
- [ ] دکمه روز بعد تاریخ بازی‌ها را تغییر می‌دهد.
- [ ] انتخابگر تقویم باز می‌شود و تاریخ انتخاب می‌کند.
- [ ] بازی‌ها بر اساس لیگ گروه‌بندی شده‌اند.
- [ ] کارت بازی وارد صفحه Match Details می‌شود.
- [ ] سرچ داخل Home با نام تیم، لیگ یا کشور کار می‌کند.
- [ ] وقتی نتیجه‌ای وجود ندارد، Empty State نمایش داده می‌شود.
- [ ] هنگام لود شدن، Loading Skeleton نمایش داده می‌شود.
- [ ] وقتی دیتای نمونه نمایش داده می‌شود، Demo banner دیده می‌شود.

## صفحه Live

- [ ] صفحه Live بدون کرش باز می‌شود.
- [ ] بازی‌های زنده بر اساس لیگ گروه‌بندی می‌شوند.
- [ ] کارت بازی وارد Match Details می‌شود.
- [ ] وقتی بازی زنده‌ای نیست، Empty State نمایش داده می‌شود.
- [ ] هنگام لود شدن، Loading Skeleton نمایش داده می‌شود.
- [ ] وقتی دیتای نمونه Live نمایش داده می‌شود، Demo banner دیده می‌شود.

## صفحه Leagues

- [ ] لیگ‌های معروف نمایش داده می‌شوند.
- [ ] لوگوی لیگ‌ها درست نمایش داده می‌شود.
- [ ] سرچ لیگ‌ها در لیست لیگ‌های معروف کار می‌کند.
- [ ] باز کردن یک لیگ وارد صفحه League Details می‌شود.

## صفحه League Details

- [ ] تب Overview اطلاعات پروفایل لیگ را نشان می‌دهد.
- [ ] Overview آخرین قهرمان و تعداد رکوردهای History را نشان می‌دهد.
- [ ] تب Fixtures بازی‌ها یا دیتای نمونه را نشان می‌دهد.
- [ ] انتخاب Round کار می‌کند.
- [ ] کارت بازی وارد Match Details می‌شود.
- [ ] تب Standings جدول را نمایش می‌دهد.
- [ ] کلیک روی تیم در جدول وارد Team Details می‌شود.
- [ ] تب History قهرمان‌های دوره‌های قبل را نشان می‌دهد.
- [ ] انتخاب فصل در History نتیجه را فیلتر می‌کند.
- [ ] جام جهانی از فصل 2022 استفاده می‌کند.

## صفحه Match Details

- [ ] تب Summary اطلاعات ورزشگاه، داور، تعداد events و تعداد lineups را نشان می‌دهد.
- [ ] رویدادهای بازی بر اساس دقیقه مرتب هستند.
- [ ] تب Stats آمار دو تیم را نشان می‌دهد، اگر داده موجود باشد.
- [ ] تب Lineups ترکیب، مربی، بازیکنان اصلی و ذخیره‌ها را نشان می‌دهد، اگر داده موجود باشد.
- [ ] لوگوی تیم‌ها در score card وارد Team Details می‌شود.
- [ ] اگر events، stats یا lineups موجود نباشند، Empty State مناسب نمایش داده می‌شود.
- [ ] Match Details برای بازی‌های demo هم باز می‌شود.

## صفحه Team Details

- [ ] هدر تیم لوگو، نام، کشور و دکمه favorite را نشان می‌دهد.
- [ ] تب Overview فرم اخیر، اطلاعات باشگاه، ورزشگاه، ظرفیت و مربی را نشان می‌دهد.
- [ ] تب Matches بازی‌های اخیر تیم را نشان می‌دهد.
- [ ] ردیف بازی‌های اخیر وارد Match Details می‌شود.
- [ ] تب Lineup آخرین ترکیب و بازیکنان را نشان می‌دهد.
- [ ] تب Squad لیست بازیکنان را نشان می‌دهد.
- [ ] وقتی API در دسترس نیست و Demo fallback روشن است، Team Details با دیتای نمونه باز می‌شود.

## صفحه Search

- [ ] Search به صورت پیش‌فرض روی Teams است.
- [ ] قبل از تایپ 3 حرف، تیم‌های معروف نمایش داده می‌شوند.
- [ ] با تایپ 3 حرف یا بیشتر، سرچ تیم‌ها انجام می‌شود.
- [ ] با تغییر حالت به Leagues، سرچ لیگ‌ها انجام می‌شود.
- [ ] اگر چند کشور وجود داشته باشد، چیپ فیلتر کشور نمایش داده می‌شود.
- [ ] فیلتر کشور نتایج را محدود می‌کند.
- [ ] سرچ‌های اخیر ذخیره می‌شوند.
- [ ] سرچ‌های اخیر پاک می‌شوند.
- [ ] Recently viewed در صورت فعال بودن تنظیمات نمایش داده می‌شود.
- [ ] وقتی نتیجه‌ای نیست، Empty State نمایش داده می‌شود.

## صفحه Favorites

- [ ] تیم‌ها به favorite اضافه و حذف می‌شوند.
- [ ] لیگ‌ها به favorite اضافه و حذف می‌شوند.
- [ ] بازی‌ها follow و unfollow می‌شوند.
- [ ] صفحه Favorites آیتم‌های ذخیره‌شده را درست باز می‌کند.
- [ ] خلاصه favorites در Home طبق Settings نمایش داده یا مخفی می‌شود.

## صفحه Settings

- [ ] کارت Data source وضعیت Real API را نشان می‌دهد.
- [ ] کارت Data source وضعیت Demo fallback را نشان می‌دهد.
- [ ] سوییچ Demo fallback بعد از بستن و باز کردن برنامه حفظ می‌شود.
- [ ] سوییچ Favorites summary روی Home اثر می‌گذارد.
- [ ] سوییچ Recently viewed روی Search اثر می‌گذارد.
- [ ] سوییچ Match alert controls روی کنترل‌های کارت بازی اثر می‌گذارد.

## تست Demo و Offline

- [ ] با `API_ENABLED=false` و Demo fallback روشن، Home دیتای نمونه نشان می‌دهد.
- [ ] با `API_ENABLED=false` و Demo fallback روشن، Live دیتای نمونه نشان می‌دهد.
- [ ] با `API_ENABLED=false` و Demo fallback روشن، Search تیم و لیگ نمونه نشان می‌دهد.
- [ ] با `API_ENABLED=false` و Demo fallback خاموش، برنامه خطا یا Empty State دوستانه نشان می‌دهد.

## کیفیت نهایی

- [ ] خطای خام API به کاربر نمایش داده نمی‌شود.
- [ ] فایل `.env` و API key واقعی commit نشده‌اند.
- [ ] `flutter analyze` بدون خطا اجرا می‌شود یا warningهای باقی‌مانده مشخص شده‌اند.
- [ ] UI روی Chrome درست نمایش داده می‌شود.
- [ ] UI روی دستگاه هدف، موبایل یا دسکتاپ، درست نمایش داده می‌شود.
