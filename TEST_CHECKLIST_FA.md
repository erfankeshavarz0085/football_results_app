# چک‌لیست تست دستی Shoot Ball

این چک‌لیست را پس از تغییرات مهم و پیش از انتشار اجرا کنید. در صورت امکان، هم حالت API واقعی و هم حالت آفلاین/دمو را تست کنید.

## محیط و شروع برنامه

- [ ] فایل `.env` به‌صورت محلی وجود دارد و در Git ثبت نشده است.
- [ ] برای تست درخواست واقعی، `API_KEY` معتبر تنظیم شده است.
- [ ] `API_ENABLED=true` درخواست‌های واقعی API را فعال می‌کند.
- [ ] `API_ENABLED=false` از ارسال درخواست واقعی جلوگیری می‌کند.
- [ ] `API_DEMO_FALLBACK_ENABLED=true` گزینهٔ دمو را در تنظیمات برنامه در دسترس قرار می‌دهد.
- [ ] صفحهٔ Splash نمایش داده می‌شود و برنامه بدون خطا وارد Home می‌شود.
- [ ] هر پنج بخش نوار پایین کار می‌کنند: Home، Leagues، Live، Search و Favorites.

## Home و Live

- [ ] Home بازی‌های تاریخ انتخاب‌شده را دریافت و بر اساس لیگ گروه‌بندی می‌کند.
- [ ] دکمه‌های روز قبل/بعد و انتخاب‌گر تقویم تاریخ بازی‌ها را تغییر می‌دهند.
- [ ] فیلتر Home نام تیم، لیگ یا کشور را درست پیدا می‌کند.
- [ ] با باز کردن یک بازی، صفحهٔ Match Details نمایش داده می‌شود و بازی در Recently Viewed ذخیره می‌شود.
- [ ] حالت‌های بارگذاری، خالی، خطا و دمو واضح و قابل استفاده هستند.
- [ ] بخش Live بدون کرش باز می‌شود، بازی‌ها را بر اساس لیگ گروه‌بندی می‌کند و Match Details را باز می‌کند.
- [ ] هنگام نمایش دادهٔ دمو، نوار اطلاع‌رسانی دمو دیده می‌شود.

## لیگ‌ها

- [ ] فهرست لیگ‌های معروف، لوگوها و جست‌وجوی لیگ درست کار می‌کنند.
- [ ] با باز کردن لیگ، نام، لوگو، کشور و دکمهٔ علاقه‌مندی صحیح نمایش داده می‌شود.
- [ ] تغییر فصل، تب‌هایی را که به دادهٔ فصل وابسته‌اند به‌روزرسانی می‌کند.
- [ ] تب Overview اطلاعات رقابت را نشان می‌دهد.
- [ ] Fixtures بارگذاری می‌شود، انتخاب Round کار می‌کند و کارت بازی Match Details را باز می‌کند.
- [ ] Standings بارگذاری می‌شود و انتخاب یک تیم، Team Details را باز می‌کند.
- [ ] آمار بازیکنان در تب‌های Goals، Assists و Clean sheets بارگذاری می‌شود و حالت خالی/تلاش مجدد درست است.
- [ ] History قهرمانان را نشان می‌دهد و فیلتر فصل کار می‌کند.
- [ ] بازی‌های World Cup از مسیر دادهٔ پشتیبانی‌شدهٔ سال 2022 استفاده می‌کنند.

## بازی‌ها و تیم‌ها

- [ ] Summary بازی، ورزشگاه، داور، رویدادها و ترکیب‌های موجود را نمایش می‌دهد.
- [ ] رویدادهای بازی بر اساس دقیقه مرتب هستند.
- [ ] در صورت وجود داده، Stats هر دو تیم را نشان می‌دهد.
- [ ] در صورت وجود داده، Lineups آرایش، مربی، بازیکنان اصلی و ذخیره را نشان می‌دهد.
- [ ] لوگوی تیم‌ها در Match Details، صفحهٔ Team Details همان تیم را باز می‌کند.
- [ ] صفحهٔ تیم، اطلاعات هدر، فرم/اطلاعات، بازی‌ها، ترکیب و Squad را در صورت وجود داده نشان می‌دهد.
- [ ] انتخاب یک بازی اخیر تیم، Match Details را باز می‌کند.
- [ ] نبود دادهٔ API به‌جای کرش، حالت خالی یا خطای قابل‌فهم نشان می‌دهد.

## جست‌وجو و بازیکنان

- [ ] Search به‌صورت پیش‌فرض روی Teams است و پیش از وارد کردن سه حرف، تیم‌های محبوب را نمایش می‌دهد.
- [ ] حالت‌های Teams، Leagues و Players پس از وارد کردن حداقل سه حرف جست‌وجو می‌کنند.
- [ ] در صورت نیاز چیپ‌های فیلتر کشور نمایش داده می‌شوند و نتیجهٔ حالت فعال را فیلتر می‌کنند.
- [ ] جست‌وجوهای اخیر ذخیره، قابل استفادهٔ دوباره و قابل پاک‌کردن هستند.
- [ ] Recently Viewed فقط در صورت فعال‌بودن گزینهٔ Settings نمایش داده می‌شود.
- [ ] انتخاب بازیکن، Player Details را باز می‌کند.
- [ ] Player Details اطلاعات موجود پروفایل، سابقهٔ باشگاهی و افتخارات را نشان می‌دهد.
- [ ] حالت‌های خطا، بازیکن پیدا نشد و تلاش مجدد درست کار می‌کنند.

## علاقه‌مندی‌ها و تنظیمات

- [ ] تیم، لیگ، بازیکن و بازی را می‌توان به علاقه‌مندی‌ها افزود یا حذف کرد.
- [ ] فیلترهای Favorites شامل All، Matches، Leagues، Teams و Players درست عمل می‌کنند.
- [ ] علاقه‌مندی‌های ذخیره‌شده بعد از راه‌اندازی مجدد باقی می‌مانند.
- [ ] نمایش خلاصهٔ علاقه‌مندی‌ها در Home از تنظیم مربوط پیروی می‌کند.
- [ ] نمایش Recently Viewed از تنظیم مربوط پیروی می‌کند.
- [ ] نمایش کنترل‌های هشدار بازی از تنظیم مربوط پیروی می‌کند.
- [ ] کلید Demo fallback بعد از راه‌اندازی مجدد باقی می‌ماند.

## پایداری و کنترل انتشار

- [ ] با غیرفعال‌بودن API واقعی و فعال‌بودن دمو، مسیرهای Home، Live، Search، لیگ، تیم و بازی با دادهٔ دمو در بخش‌های پشتیبانی‌شده قابل استفاده‌اند.
- [ ] با غیرفعال‌بودن API واقعی و دمو، برنامه پیام خطا یا حالت خالیِ مناسب نمایش می‌دهد.
- [ ] پس از راه‌اندازی مجدد، تنظیمات، علاقه‌مندی‌ها، بازی‌های دنبال‌شده و موارد اخیراً دیده‌شده حفظ می‌شوند.
- [ ] هیچ استثنای خام یا کلید API به کاربر نشان داده نمی‌شود و کلید API در Git ثبت نشده است.
- [ ] دستور `flutter analyze` بدون خطا اجرا می‌شود، یا خطاهای باقی‌مانده مستند شده‌اند.
- [ ] برنامه در Chrome و دستگاه/پلتفرم هدف smoke test شده است.
